############################################################################
#          Docker Deployment Specific Environment Variables (.env)
############################################################################
#
# This file is designed specifically for the Docker Compose (docker-compose.yml) environment.
# When running in Docker mode, the frontend service will load configurations from this file,
# overriding any same-name settings in 'frontend/yaml/config.yaml'.
#
# !!! Note for Standalone Frontend Users on Windows !!!
# If you are not using Docker, do not modify this file.
# Your configuration should be done in the 'frontend/yaml/config.yaml' file instead.
############################################################################

# [Docker Internal Path] Path to ComfyUI inside the container (usually no modification needed)
COMFYUI_PATH=/app/ComfyUI

# [Docker Internal Communication]
# Use the 'COMFYUI_BACKEND_' prefix to define backends.
# The part after the prefix will be used as the backend's name (e.g., DEFAULT -> "default", 3D_BACKEND -> "3d_backend").
# This name must correspond to the 'target_backend' value in the UI modules.
# The hostname in the URL should be the backend service name defined in docker-compose.yml.
COMFYUI_BACKEND_DEFAULT=http://backend_defalut:8188
COMFYUI_BACKEND_3D_BACKEND=http://backend_3d:8189

# [Network Proxy] Set this if your frontend needs to access the external network through a proxy.
# 'host.docker.internal' points to the host machine's 127.0.0.1 or localhost from within the Docker container.
HTTP_PROXY=http://host.docker.internal:10808
HTTPS_PROXY=http://host.docker.internal:10808

# [Docker Network] The address the frontend listens on inside the container (0.0.0.0 means listen on all network interfaces)
GRADIO_SERVER_NAME=0.0.0.0

# [Optional] HuggingFace model cache path (mapped as a volume in docker-compose.yml)
HF_CACHE_PATH=/root/.cache/huggingface